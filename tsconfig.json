{
  "extends": "@tsconfig/svelte/tsconfig.json",

  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": ".",
    "paths": {
      "$icons": ["src/icons"],
      "$utils": ["src/utils"],
      "$components": ["src/components"],
    },
  },

  "include": ["src/**/*"],
  "exclude": ["node_modules/*", "public/*"]
}